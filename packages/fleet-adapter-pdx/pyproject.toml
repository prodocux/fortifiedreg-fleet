[project]
name = "fleet-adapter-pdx"
version = "0.3.0"
description = "PDX Artifact Engine adapter for Fortified Enterprise Fleet"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7.0",
    "jsonschema>=4.21.0",
    "pdx-artifact-core @ git+https://github.com/prodocux/pdx-artifact-engine.git@55a9293c8d5c0091e04e457dc43f662058e50068#subdirectory=packages/pdx_artifact_core",
    "fleet-governance-core",
    "fleet-domain-cosmetics",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/fleet_adapter_pdx"]
